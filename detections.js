function checkurl(URL) {
    if (URL == "https://bitproxy.github.io/404.html") {
        return "This url exists, used for non-existance files."
    }
    return "File cannot be found."
}

// More functions adding sooner..